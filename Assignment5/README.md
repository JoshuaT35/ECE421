# ECE421 Assignment 5

## How to run main

### To run `main()`
- For question 1, run `cargo run --package question1`.
- For question 2, run `cargo run --package question2`.
- For question 3, run `cargo run --package question3`.

## Question 1 Answers

(a) The program takes in two parameters, `text` and `string`, and stores all lines and the index(es) of each line in `string` where `text` appears.

(b) The output for the given input is:
```
x: 0, y: 16
x: 2, y: 21
x: 4, y: 18
x: 12, y: 23
x: 13, y: 42
```

## NOTES
`delete()` in question 3 not implemented.